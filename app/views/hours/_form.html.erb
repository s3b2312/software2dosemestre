<%= form_with(model: hour, local: true) do |form| %>
  <% if hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hour.errors.count, "error") %> prohibited this hour from being saved:</h2>

      <ul>
      <% hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :identificador %>
    <%= form.text_field :identificador, id: :hour_identificador %>
  </div>

  <div class="field">
    <%= form.label :tiempo_ini %>
    <%= form.text_field :tiempo_ini, id: :hour_tiempo_ini %>
  </div>

  <div class="field">
    <%= form.label :tiempo_ter %>
    <%= form.text_field :tiempo_ter, id: :hour_tiempo_ter %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
