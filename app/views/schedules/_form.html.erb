<%= form_with(model: schedule, local: true) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero %>
    <%= form.text_field :numero, id: :schedule_numero %>
  </div>

  <div class="field">
    <%= form.label :rut_esp %>
    <%= form.text_field :rut_esp, id: :schedule_rut_esp %>
  </div>

  <div class="field">
    <%= form.label :hora %>
    <%= form.text_field :hora, id: :schedule_hora %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
